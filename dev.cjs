// Local development server: GROQ_API_KEYS and DND_SESSION_SECRET in environment.
const http=require('node:http'),fs=require('node:fs'),path=require('node:path');const handler=require('./api/turn.js');
http.createServer(async(req,res)=>{const url=new URL(req.url,'http://localhost');if(url.pathname==='/api/turn'){let raw='';for await(const chunk of req){raw+=chunk;if(raw.length>100000){res.writeHead(413);res.end();return}}req.body=raw||undefined;res.status=n=>{res.statusCode=n;return res};res.json=v=>{res.setHeader('Content-Type','application/json');res.end(JSON.stringify(v))};return handler(req,res)}const file=path.resolve('dist','.'+(url.pathname==='/'?'/index.html':url.pathname));if(!file.startsWith(path.resolve('dist')+path.sep)){res.writeHead(403);res.end();return}try{const data=fs.readFileSync(file);res.setHeader('Content-Type',({'.html':'text/html','.js':'text/javascript','.css':'text/css'})[path.extname(file)]||'application/octet-stream');res.end(data)}catch{res.writeHead(404);res.end('Not found')}}).listen(3000,()=>console.log('Game ready at http://localhost:3000'));
